# Workflow CI/CD E1 — tests, build Docker, deploy (main/develop).
# Dernière mise à jour : 2026-05-19
name: DataSens E1 CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
    # Ne pas lancer la CI sur les commits limités à docs/reports/presentations.
    paths-ignore:
      - 'reports/**'
      - 'docs/**'
      - 'presentations/**'
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

# Un seul run à la fois par branche (limite les appels API Actions / rate limit).
concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: true

# Requis pour actions/checkout (évite "could not read Username" sur dépôt privé).
permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Lint & Test
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ github.token }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest
      
      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=${{ github.workspace }}:${{ github.workspace }}/src:$PYTHONPATH" >> $GITHUB_ENV
      
      - name: Run E1 isolation tests (fast)
        run: pytest tests/test_e1_isolation.py -v -m "not slow"
      
      - name: Validate E1 pipeline structure
        run: |
          python -c "from src.e1.pipeline import E1Pipeline; print('✅ E1Pipeline importable')"
          python -c "from src.shared.interfaces import E1DataReader; print('✅ E1DataReader importable')"
      
      - name: Run other tests (E2/E3 + auth + lineage)
        run: pytest tests/ -v --ignore=tests/test_e1_isolation.py

  # Build Docker Image
  build:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ github.token }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
            VCS_REF=${{ github.sha }}
      
      - name: Verify Docker image
        run: |
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:main || echo "Image not pushed (PR)"
          echo "✅ Docker build successful"

  # Deploy (only on main branch)
  deploy:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ github.token }}

      - name: Deploy to production
        run: |
          echo "Deployment would happen here"
          echo "Example: docker-compose up -d"
          # Add your deployment commands here

